[project]
name = "secretvault"
version = "0.1.0"

[project.scripts]
vault = "cli.main:app"

[tool.setuptools]
packages = ["cli", "backend"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["B008"]